pub mod interfaces {
    pub mod imarketplace;
    pub mod iownership;
    pub mod iverify_sig;
    pub mod itipping;  // Added new tipping interface(del)
}
pub mod marketplace;
pub mod ownership;
pub mod verify_sig;
pub mod tipping;  // Added new tipping module (del)